# siting
Multiple observer siting

## To compile
* All: `make`
* CUDA: `make cuda`
* OpenMP: `make omp`
* Sequential: `make seq`

## To run
`.program nrows, roi, oht/tht, ntests, blocksize, nwanted, intervis, infile, outfile`
